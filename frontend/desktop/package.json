{
  "name": "my-kanban-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Electron shell for the My Kanban local desktop edition. The renderer is the same Angular app, built with the `desktop` configuration (ng build --configuration desktop) and loaded from the file system.",
  "main": "main.cjs",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^33.0.0"
  },
  "allowScripts": {
    "electron@33.4.11": true
  }
}

# how to run

For this project I used Marven 3.9.9 and Java 21, I use the Marven Wrapper to run with:

`./mvnw spring-boot:run`

For those that doesnt needs this workaround just simply pass this command:

`mvn spring-boot:run`